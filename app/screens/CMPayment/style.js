/** @format */

import { StyleSheet } from 'react-native'
const styles = StyleSheet.create({
  mainCMBottomNav: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    width: '100%',
    zIndex: 22,
  },
})
