import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    addInvenBtn: {
        marginBottom: 40,
    },
    textArea: {
        paddingTop: 15,
        height: 100,
        color: 'black'
    }
})