import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    itemWrap: {
        backgroundColor: '#ffffff', 
        borderRadius: 5,
        marginVertical : 10,
        borderBottomWidth: 1,
        minHeight: 60,
        borderColor: '#f0f0f0',
        marginHorizontal: 15,
        // flex:1
    }
})